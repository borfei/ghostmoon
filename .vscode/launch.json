{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Jekyll site",
            "type": "node-terminal",
            "request": "launch",
            "command": "bundle exec jekyll serve",
            "cwd": "${workspaceFolder}"
        }
    ]
}